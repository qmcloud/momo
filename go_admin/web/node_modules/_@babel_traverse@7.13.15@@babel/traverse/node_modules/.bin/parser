#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_@babel_parser@7.13.15@@babel/parser/bin/babel-parser.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_@babel_parser@7.13.15@@babel/parser/bin/babel-parser.js" "$@"
  ret=$?
fi
exit $ret
