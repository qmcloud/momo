#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_cli-highlight@2.1.11@cli-highlight/bin/highlight" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_cli-highlight@2.1.11@cli-highlight/bin/highlight" "$@"
  ret=$?
fi
exit $ret
