#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_terser@4.8.0@terser/bin/terser" "$@"
  ret=$?
else 
  node  "$basedir/../../../_terser@4.8.0@terser/bin/terser" "$@"
  ret=$?
fi
exit $ret
