#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_esprima@4.0.1@esprima/bin/esvalidate.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_esprima@4.0.1@esprima/bin/esvalidate.js" "$@"
  ret=$?
fi
exit $ret
